# ── SQLite（默认，零配置）──
# 不设置 DATABASE_URL 即可，启动脚本自动使用 SQLite
# 数据文件保存在 backend/data/tracesession.db

# ── PostgreSQL（生产环境）──
# 取消注释并填写你的连接串：
# DATABASE_URL="postgresql://user:password@host:5432/tracesession"

# ── 端口（可选，默认 27890/27891）──
# HTTP_PORT=27890
# HTTPS_PORT=27891
