name: Build

on:
  push:
    branches: ["*"]
    tags: ["v*"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17 & 21
        uses: actions/setup-java@v4
        with:
          java-version: |
            17
            21
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x TraceSession/gradlew

      - name: Build
        run: cd TraceSession && ./gradlew build

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tracesession-${{ github.ref_name }}
          path: TraceSession/build/libs/tracesession-*.jar

  release:
    if: startsWith(github.ref_name, 'v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17 & 21
        uses: actions/setup-java@v4
        with:
          java-version: |
            17
            21
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x TraceSession/gradlew

      - name: Build Release
        run: cd TraceSession && ./gradlew build -Pmod_version=${GITHUB_REF_NAME#v}

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: TraceSession ${{ github.ref_name }}
          body: |
            See [CHANGELOG](CHANGELOG.md) for details.
          files: TraceSession/build/libs/tracesession-*.jar
          generate_release_notes: true
